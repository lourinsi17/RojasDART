class Contact {
  String? firstName;
  String? lastName;
  String? phoneNumber;
  String? address;
  int id;

  Contact(
      this.firstName, this.lastName, this.phoneNumber, this.address, this.id);
}
